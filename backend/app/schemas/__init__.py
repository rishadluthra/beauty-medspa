"""Pydantic response schemas.

These models define the shape of data returned by the API (and serialized
to JSON for the Next.js frontend). They mirror the return shapes produced
by `app.repositories` functions rather than the raw ORM models.
"""
